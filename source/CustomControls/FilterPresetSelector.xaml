<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="ItemFilterQuickPreset" TargetType="CheckBoxEx">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBoxEx">
                    <Grid Margin="-48,0,48,-48">
                        <!-- Larger focus background layer -->
                        <Border x:Name="FocusLayer"
                                Background="Transparent"
                                Margin="18,2,1,2" 
                                Padding="18" 
                                CornerRadius="100" />

                        <!-- Main content layer -->
                        <Border Background="{TemplateBinding Background}"
                                Margin="24,8,7,8" 
                                Padding="12" 
                                CornerRadius="100">
                            <Grid>
                                <!-- Grid layout for image with focus label -->
                                <Grid x:Name="ImageLayout">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <!-- Focus label - appears above image when focused -->
                                    <TextBlock Grid.Row="0" 
                                               Text="{Binding Name}" 
                                               TextAlignment="Center" 
                                               TextWrapping="Wrap" 
                                               FontSize="16"
                                               FontWeight="Bold"
                                               Foreground="{TemplateBinding Foreground}" 
                                               Margin="-64,-64,-64,4">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsFocused, RelativeSource={RelativeSource AncestorType=CheckBoxEx}}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <!-- Image container with two overlapping images -->
                                    <Grid Grid.Row="1">
                                        <!-- Default/Inactive Image -->
                                        <Image x:Name="DefaultImage"
                                               RenderOptions.BitmapScalingMode="Fant"
                                               Source="{ThemeFileBinding Name, PathFormat='Icons/Filter/Default/{0}.png'}"
                                               Stretch="Uniform"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Height="80"
                                               Width="80"
                                               Margin="-12"
                                               Visibility="Visible">
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Source}"
                                                                     Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                        </Image>

                                        <!-- Active Image -->
                                        <Image x:Name="ActiveImage"
                                               RenderOptions.BitmapScalingMode="Fant"
                                               Source="{ThemeFileBinding Name, PathFormat='Icons/Filter/Active/{0}.png'}"
                                               Stretch="Uniform"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Height="80"
                                               Width="80"
                                               Margin="-12"
                                               Visibility="Collapsed">
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Source}"
                                                                     Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                            <Image.Effect>
                                                <DropShadowEffect Color="Black" 
                                                     Direction="315" 
                                                     ShadowDepth="3" 
                                                     Opacity="0.5" 
                                                     BlurRadius="10"/>
                                            </Image.Effect>
                                        </Image>
                                    </Grid>
                                </Grid>

                                <!-- Text that stays present but becomes visually hidden when image exists -->
                                <TextBlock x:Name="TextContent" 
                                           FontFamily="{DynamicResource FontDefault}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource TextBlockBaseStyle}">
                                            <Style.Triggers>
                                                <!-- Show text if both images are null -->
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding ElementName=DefaultImage, Path=Source}" Value="{x:Null}"/>
                                                        <Condition Binding="{Binding ElementName=ActiveImage, Path=Source}" Value="{x:Null}"/>
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Opacity" Value="1"/>
                                                    <Setter Property="Width" Value="Auto"/>
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                            <!-- Default state when images exist - visually hidden but still present -->
                                            <Setter Property="Opacity" Value="0"/>
                                            <Setter Property="Width" Value="0"/>
                                        </Style>
                                    </TextBlock.Style>
                                    <Grid>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Visibility="Hidden"/>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" HorizontalAlignment="Center"
                                                       Foreground="{TemplateBinding Foreground}" />
                                            <TextBlock Text="&#x25CF;" Style="{DynamicResource TextBlockBaseStyle}"                                               
                                                       FontSize="12" Margin="0,0,0,-14"
                                                       HorizontalAlignment="Center"
                                                       x:Name="SelectionBullet" Visibility="Hidden"/>
                                        </StackPanel>                                
                                    </Grid>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Background" Value="#1AFFFFFF" TargetName="FocusLayer" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="FontWeight" Value="SemiBold" TargetName="TextContent" />
                            <Setter Property="Visibility" Value="Visible" TargetName="SelectionBullet" />
                            <!-- Hide default image and show active image when checked -->
                            <Setter Property="Visibility" Value="Collapsed" TargetName="DefaultImage" />
                            <Setter Property="Visibility" Value="Visible" TargetName="ActiveImage" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type FilterPresetSelector}">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type FilterPresetSelector}">
                    <ItemsControlEx x:Name="PART_ItemsFilterPresets" Focusable="False"
                                    KeyboardNavigation.DirectionalNavigation="Once"
                                    HorizontalLayout="True" Margin="0,0,24,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" Margin="48,0,0,0"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.Template>
                            <ControlTemplate>
                                <Border x:Name="PadderBorder" Margin="48,0,0,0">
                                    <ScrollViewer Focusable="False"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Disabled"
                                              CanContentScroll="True" Margin="-48,-48, -48, 0">
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </ControlTemplate>
                        </ItemsControl.Template>
                    </ItemsControlEx>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>