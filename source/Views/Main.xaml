<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    mc:Ignorable="d"
                    xmlns:pbeh="clr-namespace:Playnite.Behaviors;assembly=Playnite">

    <Style x:Key="MoDataWindowStyle" TargetType="ContentControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="960" />
                        </Grid.ColumnDefinitions>
                        <Border Background="{DynamicResource OverlayMenuBackgroundBrush}"
                                Grid.Column="1"
                                Width="977"
                                HorizontalAlignment="Left"
                                Margin="0,16,-17,16"
                                CornerRadius="16"
                                Padding="16,5,49,16"
                                BorderBrush="{DynamicResource DefaultBorderBrush}"
                                BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" 
                                                  Direction="315" 
                                                  ShadowDepth="5" 
                                                  Opacity="0.5" 
                                                  BlurRadius="10"/>
                            </Border.Effect>
                            <StackPanel Orientation="Vertical" pbeh:FocusBahaviors.OnVisibilityFocus="True" pbeh:FocusBahaviors.FocusBinding="True">
                                <StackPanel Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Right"
                                            Grid.Column="5">
                                    
                                    <TextBlock x:Name="PART_MoDetailsTextBatteryPercentage"
                                               Style="{DynamicResource TextBlockBaseStyle}"
                                               VerticalAlignment="Center"
                                               Margin="32,0,0,0"
                                               FontSize="64" Text="{PluginSettings Plugin=MoData, Path=PercentChargeString}"/>
                                    <Grid>
                                        <ContentControl x:Name="PART_MoDetailsElemBatteryStatus"
                                                    Focusable="False"
                                                    FontSize="160"
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,10,0">
                                            <ContentControl.Style>
                                                <Style TargetType="{x:Type ContentControl}">
                                                    <Setter Property="Foreground"
                                                        Value="{DynamicResource TextBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=BatteryCharge}"
                                                                 Value="Critical">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource DetailsBatteryStatusCritical}"/>
                                                            <Setter Property="Foreground"
                                                                Value="{DynamicResource WarningBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=BatteryCharge}"
                                                                 Value="Low">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource DetailsBatteryStatusLow}"/>
                                                            <Setter Property="Foreground"
                                                                Value="{DynamicResource AlertBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=BatteryCharge}"
                                                                 Value="Medium">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource DetailsBatteryStatusMedium}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=BatteryCharge}"
                                                                 Value="High">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource DetailsBatteryStatusHigh}"/>
                                                            <Setter Property="Foreground"
                                                                Value="{DynamicResource SuccessBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=IsCharging}"
                                                                 Value="True">
                                                            <Setter Property="Opacity"
                                                                Value="0"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ContentControl.Style>
                                        </ContentControl>
                                        <ContentControl x:Name="MoDetailsBatteryCharger"
                                                    Focusable="False"
                                                    FontSize="160"
                                                    VerticalAlignment="Center"
                                                    Margin="0,0,10,0">
                                            <ContentControl.Style>
                                                <Style TargetType="{x:Type ContentControl}">
                                                    <Setter Property="Foreground"
                                                        Value="{DynamicResource TextBrush}"/>
                                                    <Setter Property="Visibility"
                                                        Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=IsCharging}"
                                                                 Value="True">
                                                            <Setter Property="Visibility"
                                                                Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=BatteryCharge}"
                                                                 Value="Critical">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource DetailsBatteryStatusChargingCritical}"/>
                                                            <Setter Property="Foreground"
                                                                Value="{DynamicResource AlertBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=BatteryCharge}"
                                                                 Value="Low">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource DetailsBatteryStatusChargingLow}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=BatteryCharge}"
                                                                 Value="Medium">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource DetailsBatteryStatusChargingMedium}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=BatteryCharge}"
                                                                 Value="High">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource DetailsBatteryStatusChargingHigh}"/>
                                                            <Setter Property="Foreground"
                                                                Value="{DynamicResource SuccessBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ContentControl.Style>
                                        </ContentControl>
                                    </Grid>
                                    <TextBlock x:Name="PART_MoDetailsTextClock"
                                               Style="{DynamicResource TextBlockBaseStyle}"
                                               VerticalAlignment="Center"
                                               Margin="36,0,40,0"
                                               FontSize="64" Text="{PluginSettings Plugin=MoData, Path=Time}"/>
                                </StackPanel>
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}" Margin="0,64,0,16">Network Connectivity</TextBlock>
                                <ButtonEx Margin="0,16,0,16" Command="{PluginSettings Plugin=MoData, Path=OpenNetworkSettings}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock x:Name="MoDetails_Wifi"
                                               FontFamily="Segoe Fluent Icons"
                                               FontSize="96"
                                               VerticalAlignment="Center"
                                               Margin="8,0,10,0">
                                            <TextBlock.Style>
                                                <Style TargetType="{x:Type TextBlock}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{PluginStatus Plugin=MoData_5a0a672a-998a-4cf3-94ea-44543de5afcb, Status=Installed}"
                                                                 Value="False">
                                                            <Setter Property="Visibility"
                                                                Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="Full">
                                                            <Setter Property="Text"
                                                                Value="&#xe701;"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="Strong">
                                                            <Setter Property="Text"
                                                                Value="&#xe874;"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="Medium">
                                                            <Setter Property="Text"
                                                                Value="&#xe873;"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="Weak">
                                                            <Setter Property="Text"
                                                                Value="&#xe872;"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="None">
                                                            <Setter Property="Text"
                                                                Value="&#xf384;"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="Wired">
                                                            <Setter Property="Text"
                                                                Value="&#xe774;"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBlock Text="{PluginSettings Plugin=MoData, Path=NetworkName}" FontSize="64" Margin="32,0,0,0"/>
                                    </StackPanel>
                                </ButtonEx>
                                <TextBlock Style="{DynamicResource TextBlockBaseStyle}" Margin="0,64,0,16">Storage</TextBlock>
                                <ItemsControl ItemsSource="{PluginSettings Plugin=MoData, Path=DiskUsages}" Focusable="False">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,8,0,8"
                      Visibility="{Binding Converter={StaticResource NullToVisibilityConverter}}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="96" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <!-- Letter and Capacity -->
                                                <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center" Margin="0,0,32,0">
                                                    <TextBlock Text="{Binding Label, Mode=OneWay}"  Foreground="{DynamicResource TextBrush}"
                                   FontSize="32" 
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"/>

                                                    <TextBlock Text="{Binding TotalSpaceString, Mode=OneWay}" 
                                                                           FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" Foreground="{DynamicResource TextBrushDark}"/>
                                                </StackPanel>

                                                <!-- Disk Info -->
                                                <StackPanel Grid.Column="1" Margin="16,8,8,8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="*" />
                                                        </Grid.ColumnDefinitions>

                                                        <TextBlock Grid.Column="0" 
                                                                       Text="{Binding UsedPercentageString, Mode=OneWay}" 
                                                                       FontSize="16" FontWeight="Bold" 
                                                                       VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>

                                                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                                            <TextBlock Text="Free Space: " FontSize="14" Margin="64,0,0,0" 
                                                                       VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                                            <TextBlock Text="{Binding FreeSpaceString, Mode=OneWay}" 
                                                                           FontSize="14" FontWeight="SemiBold" 
                                                                       VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                                                        </StackPanel>
                                                    </Grid>

                                                    <ProgressBar Value="{Binding UsedPercentage, Mode=OneWay}"
                                                                 Style="{DynamicResource BarRound16}"
                                     Maximum="100" 
                                     Height="32" HorizontalAlignment="Stretch"
                                     Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <ButtonEx Margin="0,16,0,16" Command="{PluginSettings Plugin=MoData, Path=OpenAppsSettings}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock
                                               FontFamily="Segoe Fluent Icons"
                                               FontSize="96"
                                               VerticalAlignment="Center"
                                               Margin="8,0,10,0" Text="&#xeda2;" />
                                        <TextBlock Text="Manage Apps and Games" FontSize="64" Margin="32,0,0,0"/>
                            </StackPanel>
                            </ButtonEx>
                            </StackPanel>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style TargetType="{x:Type Main}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Main}">
                    <Grid x:Name="PART_MainHost"
                          d:DesignWidth="{StaticResource DesignWidth}"
                          d:DesignHeight="{StaticResource DesignHeight}"
                          d:DataContext="{x:Static DesignMainViewModel.DesignIntance}">
                        <Grid x:Name="PART_ViewHost"
                              Background="{DynamicResource MainBackgourndBrush}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <FadeImage x:Name="PART_ImageBackground"
                                       Grid.RowSpan="5"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Top"/>
                            <Grid Grid.Row="0"
                                  Height="80"
                                  Margin="0,24">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="24"/>
                                </Grid.ColumnDefinitions>
                                <ButtonEx Command="{PluginSettings Plugin=NowPlaying, Path=OpenDialog}"
                                          Grid.Column="1"
                                          Margin="12,8,32,8">
                                    <ButtonEx.Style>
                                        <Style TargetType="ButtonEx"
                                               BasedOn="{StaticResource ButtonNowPlaying}">
                                            <Setter Property="Visibility"
                                                    Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{PluginStatus Plugin=NowPlaying_db4e7ade-57fb-426c-8392-60e2347a0209, Status=Installed}"
                                                             Value="False">
                                                    <Setter Property="Visibility"
                                                            Value="Collapsed"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{PluginSettings Plugin=NowPlaying, Path=IsGameRunning}"
                                                             Value="False">
                                                    <Setter Property="Visibility"
                                                            Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ButtonEx.Style>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Width="56"
                                               Height="56"
                                               Style="{DynamicResource NowPlayingImageStyle}"
                                               Source="{PluginSettings Plugin=NowPlaying, Path=RunningGame.IconPath}"/>
                                        <StackPanel Orientation="Vertical"
                                                    Margin="0,0,0,-6">
                                            <TextBlock x:Name="NowPlaying_Title"
                                                       Style="{DynamicResource NowPlayingTitleStyle}"
                                                       Text="NOW PLAYING"/>
                                            <TextBlock x:Name="NowPlaying_Game"
                                                       Style="{DynamicResource NowPlayingGameStyle}"
                                                       Text="{PluginSettings Plugin=NowPlaying, Path=RunningGame.GameName}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </ButtonEx>

                                <Border  Background="{DynamicResource TopBarBackgroundBrush}"
                                         Grid.Column="3"
                                         x:Name="PART_ElemProgressIndicator"
                                         Margin="12,8,32,8"
                                         Padding="16,0"
                                         CornerRadius="16"
                                         BorderThickness="1"
                                         BorderBrush="{DynamicResource DefaultBorderBrush}">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black"
                                                          Direction="315"
                                                          ShadowDepth="5"
                                                          Opacity="0.5"
                                                          BlurRadius="10"/>
                                    </Border.Effect>
                                    <StackPanel Orientation="Horizontal"
                                                VerticalAlignment="Center">
                                        <TextBlock x:Name="PART_TextProgressTooltip"
                                                   Margin="0,0,15,0"
                                                   FontSize="20"
                                                   VerticalAlignment="Center"
                                                   Style="{DynamicResource TextBlockBaseStyle}"/>
                                        <ContentControl Style="{DynamicResource ProgressIndicator}"
                                                        Focusable="False"
                                                        Height="38"
                                                        Width="38"
                                                        VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <ButtonEx x:Name="ShowDataButton" Grid.Column="5" VerticalAlignment="Center" Background="Transparent" Style="{DynamicResource ButtonTopMenuData}" Command="{PluginSettings Plugin=MoData, Path=MoDetails}">
                                    <StackPanel Orientation="Horizontal"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Right">
                                    <TextBlock x:Name="MoData_Wifi"
                                               FontFamily="Segoe Fluent Icons"
                                               FontSize="48"
                                               VerticalAlignment="Center"
                                               Margin="0,0,10,0"
                                               Foreground="{DynamicResource TextBrush}">
                                        <TextBlock.Style>
                                            <Style TargetType="{x:Type TextBlock}">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{PluginStatus Plugin=MoData_5a0a672a-998a-4cf3-94ea-44543de5afcb, Status=Installed}"
                                                                 Value="False">
                                                        <Setter Property="Visibility"
                                                                Value="Collapsed"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="Full">
                                                        <Setter Property="Text"
                                                                Value="&#xe701;"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="Strong">
                                                        <Setter Property="Text"
                                                                Value="&#xe874;"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="Medium">
                                                        <Setter Property="Text"
                                                                Value="&#xe873;"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="Weak">
                                                        <Setter Property="Text"
                                                                Value="&#xe872;"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="None">
                                                        <Setter Property="Text"
                                                                Value="&#xf384;"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{PluginSettings Plugin=MoData, Path=WifiStrength}"
                                                                 Value="Wired">
                                                        <Setter Property="Text"
                                                                Value="&#xe774;"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock x:Name="PART_TextBatteryPercentage"
                                               Style="{DynamicResource TextBlockBaseStyle}"
                                               VerticalAlignment="Center"
                                               Margin="32,0,0,0"
                                               FontSize="42"/>
                                    <Grid>
                                        <ContentControl x:Name="PART_ElemBatteryStatus"
                                                    Focusable="False"
                                                    FontSize="96"
                                                    VerticalAlignment="Center"
                                                    Margin="0,-8,10,0">
                                            <ContentControl.Style>
                                                <Style TargetType="{x:Type ContentControl}">
                                                    <Setter Property="Foreground"
                                                        Value="{DynamicResource TextBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding PowerStatus.Charge}"
                                                                 Value="Critical">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource BatteryStatusCritical}"/>
                                                            <Setter Property="Foreground"
                                                                Value="{DynamicResource WarningBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding PowerStatus.Charge}"
                                                                 Value="Low">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource BatteryStatusLow}"/>
                                                            <Setter Property="Foreground"
                                                                Value="{DynamicResource AlertBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding PowerStatus.Charge}"
                                                                 Value="Medium">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource BatteryStatusMedium}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding PowerStatus.Charge}"
                                                                 Value="High">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource BatteryStatusHigh}"/>
                                                            <Setter Property="Foreground"
                                                                Value="{DynamicResource SuccessBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding PowerStatus.IsCharging}"
                                                                 Value="True">
                                                            <Setter Property="Opacity"
                                                                Value="0"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ContentControl.Style>
                                        </ContentControl>
                                        <ContentControl x:Name="BatteryCharger"
                                                    Focusable="False"
                                                    FontSize="96"
                                                    VerticalAlignment="Center"
                                                    Margin="0,-8,10,0">
                                            <ContentControl.Style>
                                                <Style TargetType="{x:Type ContentControl}">
                                                    <Setter Property="Foreground"
                                                        Value="{DynamicResource TextBrush}"/>
                                                    <Setter Property="Visibility"
                                                        Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding PowerStatus.IsCharging}"
                                                                 Value="True">
                                                            <Setter Property="Visibility"
                                                                Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding PowerStatus.Charge}"
                                                                 Value="Critical">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource BatteryStatusChargingCritical}"/>
                                                            <Setter Property="Foreground"
                                                                Value="{DynamicResource AlertBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding PowerStatus.Charge}"
                                                                 Value="Low">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource BatteryStatusChargingLow}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding PowerStatus.Charge}"
                                                                 Value="Medium">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource BatteryStatusChargingMedium}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding PowerStatus.Charge}"
                                                                 Value="High">
                                                            <Setter Property="Content"
                                                                Value="{DynamicResource BatteryStatusChargingHigh}"/>
                                                            <Setter Property="Foreground"
                                                                Value="{DynamicResource SuccessBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ContentControl.Style>
                                        </ContentControl>
                                    </Grid>
                                    <TextBlock x:Name="PART_TextClock"
                                               Style="{DynamicResource TextBlockBaseStyle}"
                                               VerticalAlignment="Center"
                                               Margin="20,0,12,0"
                                               FontSize="42"/>
                                </StackPanel>
                                </ButtonEx>


                            </Grid>
                            <DockPanel Grid.Row="2">
                                <Filters x:Name="PART_ElemFilters"
                                         DockPanel.Dock="Left"
                                         Focusable="False"
                                         Margin="0,-96,-8,0"/>

                                <FiltersAdditional x:Name="PART_ElemFiltersAdditional"
                                                   DockPanel.Dock="Left"
                                                   Focusable="False"/>

                                <ContentControl x:Name="PART_ContentFilterItems"
                                                DockPanel.Dock="Left"
                                                Focusable="False"/>


                                <TextBlock Text="{Binding SelectedGame.DisplayName}"
                                           FontSize="64"
                                           FontWeight="Bold"
                                           Style="{DynamicResource TextBlockBaseStyle}"
                                           Margin="96,-72,0,16"
                                           DockPanel.Dock="Bottom">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="Black"
                                                          Direction="315"
                                                          ShadowDepth="3"
                                                          Opacity="0.5"
                                                          BlurRadius="10"/>
                                    </TextBlock.Effect></TextBlock>
                                <ListBoxEx x:Name="PART_ListGameItems"
                                           Margin="0,-64,0,0"
                                           Background="Transparent"
                                           BorderThickness="0"
                                           DockPanel.Dock="Right"
                                           KeyboardNavigation.DirectionalNavigation="Once"
                                           VirtualizingStackPanel.IsVirtualizing="True"
                                           ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                           ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
                            </DockPanel>
                            <TextBlock Grid.Row="2"
                                       FontSize="40"
                                       Text="{DynamicResource LOCNoGamesFound}"
                                       TextAlignment="Center"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock"
                                           BasedOn="{StaticResource TextBlockBaseStyle}">
                                        <Setter Property="Visibility"
                                                Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Items.Count, ElementName=PART_ListGameItems}"
                                                         Value="0">
                                                <Setter Property="Visibility"
                                                        Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <Grid Grid.Row="3"
                                  Height="140"
                                  Margin="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="24"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock FontFamily="{DynamicResource FontXboxOneController}"
                                           Grid.Column="1"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"
                                           Margin="24"
                                           FontSize="48"
                                           Opacity="0.67"
                                           Foreground="{DynamicResource TextBrush}"
                                           Text="f" />
                                <Border  Margin="10"
                                         Background="{DynamicResource TopBarBackgroundBrush}"
                                         CornerRadius="60"
                                         MinWidth="480"
                                         MaxWidth="1850"
                                         Grid.Column="2"
                                         BorderBrush="{DynamicResource DefaultBorderBrush}"
                                         BorderThickness="1">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black"
                                                          Direction="315"
                                                          ShadowDepth="5"
                                                          Opacity="0.5"
                                                          BlurRadius="10"/>
                                    </Border.Effect>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                            <ColumnDefinition Width="24"></ColumnDefinition>
                                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <ButtonEx x:Name="PART_ButtonMainMenu"
                                                  VerticalAlignment="Center"
                                                  Style="{DynamicResource ButtonTopMenu}"
                                                  Margin="32,0,16,0"
                                                  Height="72"
                                                  MinWidth="72"
                                                  Grid.Column="0">
                                            <TextBlock Style="{StaticResource FluentIconStyle}"
                                                       FontSize="52"
                                                       Margin="8,9"
                                                       Text="&#xe700;"/>
                                        </ButtonEx>
                                        <ButtonEx x:Name="PART_ButtonNotifications"
                                                  VerticalAlignment="Center"
                                                  Margin="8,0,16,0"
                                                  Height="72"
                                                  MinWidth="72"
                                                  Grid.Column="1">
                                            <ButtonEx.Style>
                                                <Style TargetType="{x:Type Button}"
                                                       BasedOn="{StaticResource ButtonTopMenu}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Api Notifications.Count}"
                                                                     Value="0">
                                                            <Setter Property="Visibility"
                                                                    Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </ButtonEx.Style>
                                            <Grid>
                                                <TextBlock Style="{StaticResource FluentIconStyle}"
                                                           FontSize="52"
                                                           Text="&#xf2a5;"
                                                           Margin="8,6"/>
                                                <TextBlock Text="●"
                                                           Foreground="{DynamicResource WarningBrush}"
                                                           HorizontalAlignment="Right"
                                                           VerticalAlignment="Bottom"
                                                           FontSize="32"
                                                           Margin="0,12,6,-4"/>
                                            </Grid>
                                        </ButtonEx>
                                        <ButtonEx x:Name="PART_ButtonProgramUpdate"
                                                  VerticalAlignment="Center"
                                                  Margin="10,0,10,0"
                                                  Height="48"
                                                  MinWidth="48"
                                                  Style="{DynamicResource ButtonTopMenu}"
                                                  Grid.Column="2">
                                            <TextBlock Text="&#xEF08;"
                                                       FontFamily="{StaticResource FontIcoFont}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       Margin="2"
                                                       Foreground="{DynamicResource GlyphLightBrush}"
                                                       FontSize="30"/>
                                        </ButtonEx>
                                        <Rectangle Width="1"
                                                   Height="48"
                                                   Fill="{DynamicResource TextBrushDark}"
                                                   Grid.Column="3"></Rectangle>
                                        <FilterPresetSelector Grid.Column="4"
                                                              x:Name="PART_FilterPresetSelector"
                                                              Margin="-12,0,0,0"/>
                                    </Grid>
                                </Border>
                                <TextBlock FontFamily="{DynamicResource FontXboxOneController}"
                                           Grid.Column="3"
                                           HorizontalAlignment="Left"
                                           VerticalAlignment="Center"
                                           Margin="24"
                                           FontSize="48"
                                           Opacity="0.67"
                                           Foreground="{DynamicResource TextBrush}"
                                           Text="z" />
                            </Grid>
                            <Grid Grid.Row="4"
                                  Height="70">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="24"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="1"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <!--Install-->
                                    <ButtonEx x:Name="PART_ButtonInstall"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCInstallGame}"/>
                                    <!--Play-->
                                    <ButtonEx x:Name="PART_ButtonPlay"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCPlayGame}"/>
                                    <!--Details-->
                                    <ButtonEx x:Name="PART_ButtonDetails"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCGameDetails}"/>
                                    <!--Options-->
                                    <ButtonEx x:Name="PART_ButtonGameOptions"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCGameOptions}"/>
                                    <!--Search-->
                                    <ButtonEx x:Name="PART_ButtonSearch"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCSearchLabel}"/>
                                    <!--Filter-->
                                    <ButtonEx x:Name="PART_ButtonFilter"
                                              Style="{DynamicResource ButtonBottomMenu}"
                                              Content="{DynamicResource LOCFilter}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2"
                                            x:Name="PART_ElemSearchActive"
                                            VerticalAlignment="Top"
                                            HorizontalAlignment="Stretch"
                                            Margin="0,15,0,0">
                                    <TextBlock  FontSize="18"
                                                TextAlignment="Center"
                                                Style="{DynamicResource TextBlockBaseStyle}"
                                                Text="{DynamicResource LOCGameListSearchResults}"/>
                                    <TextBlock  Foreground="White"
                                                TextAlignment="Center"
                                                Style="{DynamicResource TextBlockBaseStyle}"
                                                Text="{Binding AppSettings.Fullscreen.FilterSettings.Name}"/>
                                </StackPanel>

                                <TextBlock Grid.Column="2"
                                           x:Name="PART_ElemExtraFilterActive"
                                           FontSize="20"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Stretch"
                                           TextAlignment="Right"
                                           Style="{DynamicResource TextBlockBaseStyle}"
                                           Text="{DynamicResource LOCGameListExtraFiltered}"/>
                            </Grid>
                            <!--Grid <Grid.Row="0" Grid.RowSpan="5" Width="540" HorizontalAlignment="Right">
                                <Border x:Name="ToggleableView" Visibility="Collapsed" Background="{DynamicResource OverlayMenuBackgroundBrush}" Margin="16, 16, -16, 16" CornerRadius="16" Padding="16, 16, 32, 16" BorderBrush="{DynamicResource DefaultBorderBrush}" BorderThickness="1">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" 
                                         Direction="315" 
                                         ShadowDepth="5" 
                                         Opacity="0.5" 
                                         BlurRadius="10"/>
                                    </Border.Effect>
                                    -->
                            <!-- Your view content -->
                            <!--
                                    <Grid pbeh:FocusBahaviors.FocusBinding="True">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="80"/>
                                            <RowDefinition />
                                        </Grid.RowDefinitions>
                                        <ButtonEx x:Name="HideButton" Grid.Row="0" HorizontalAlignment="Right" pbeh:FocusBahaviors.OnVisibilityFocus="True" Style="{DynamicResource ButtonBigRound}"
                                          BorderThickness="2" MinWidth="22" HorizontalContentAlignment="Center" Height="48" Width="48" Margin="0,0,16,0">
                                            <ButtonEx.Triggers>
                                                <EventTrigger RoutedEvent="ButtonEx.Click">
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ToggleableView" 
                                                         Storyboard.TargetProperty="Visibility">
                                                                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Collapsed}" />
                                                            </ObjectAnimationUsingKeyFrames>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </ButtonEx.Triggers>
                                            <TextBlock Width="32" Height="32" TextAlignment="Center" Opacity="0.8" Foreground="{DynamicResource TextBrush}" FontFamily="Segoe Fluent Icons" FontSize="32" Text="&#xe8bb;"/>
                                        </ButtonEx>
                                        
                                        <TextBlock Text="Content to show/hide" Grid.Row="1"/>
                                    </Grid>
                                </Border>
                            </Grid>-->
                        </Grid>

                        <GameDetails x:Name="PART_ElemGameDetails"
                                     Grid.RowSpan="4"
                                     Grid.Row="1"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Stretch"/>

                        <GameStatus x:Name="PART_ElemGameStatus"
                                    Grid.RowSpan="5"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>